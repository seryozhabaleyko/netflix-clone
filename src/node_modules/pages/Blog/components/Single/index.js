import React from 'react';

import Author from '../Author';

import './single.scss';

function Single({ match, title }) {

    const slug = match.params.slug;

    console.log(slug);


    return (
        <main className="main">
            <div className="container">
                <article className="blog-single">
                    <h1>{title}</h1>
                    <Author />
                </article>
            </div>
        </main>
    );
}

export default Single;