import initialState from 'constants/initialState';
import * as types from 'constants/types';

export default (state = initialState.movies, { type, payload }) => {

    switch (type) {
        case types.movies.fetch.success:
            return {
                ...state,
                all: payload
            }
        default:
            return state;
    }
};