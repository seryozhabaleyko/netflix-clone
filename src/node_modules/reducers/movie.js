import initialState from 'constants/initialState';
import * as types from 'constants/types';

export default (state = initialState.movie, { type, payload }) => {
    switch (type) {
        case types.movie.fetch.success:
            return {
                ...state,
                current: payload
            };
        default:
            return state;
    }
};