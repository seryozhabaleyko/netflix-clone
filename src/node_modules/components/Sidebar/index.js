import React from 'react';

const styled = {
    position: 'sticky',
    top: '90px',
};

function Sidebar({ children }) {

    return (
        <div className="sidebar">
            <div style={styled}>
                {children}
            </div>
        </div>
    );
}

export default Sidebar;