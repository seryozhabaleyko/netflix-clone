import initialState from 'redux/constants/initialState';
import { LOADING, LOADED } from 'redux/constants/types/loading';

export default (state = initialState.loading, { type, payload }) => {

    switch (type) {
        case LOADING:
            return true;
        case LOADED:
            return false;
        default:
            return state;
    }
};