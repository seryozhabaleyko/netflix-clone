import initialState from 'constants/initialState';
import * as types from 'constants/types';

export default (state = initialState.blog, { type, payload }) => {

    switch (type) {
        case types.blog.success:
            return { ...state, data: payload };
        default:
            return state;
    }
};