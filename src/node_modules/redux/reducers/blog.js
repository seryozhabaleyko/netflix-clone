import initialState from 'redux/constants/initialState';
import types from 'redux/constants/types/blog';

export default (state = initialState.blog, { type, payload }) => {

    switch (type) {
        case types.get.request:
            return { ...state, isFetching: true };
        case types.get.success:
            return { ...state, isFetching: false, data: payload };
        case types.get.failure:
            return { ...state, isFetching: false, error: payload };
        default:
            return state;
    }
};